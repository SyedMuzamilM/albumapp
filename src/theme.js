import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  typography: {
    h2: {
      fontSize: "1.2rem",
      fontWeight: "bold",
    },
    h4: {
      fontSize: "1rem",
    },
    body1: {
      color: '#91919F',
      fontSize: '0.8rem'
    },
  },
});

